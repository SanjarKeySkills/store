import React from 'react';

const Footer = () => {
	return (
		<div>
			Footer
		</div>
	);
};

export default Footer;
// export & import - это директивы
// через данные директивы загружаются модули (это скрипты, файлы)
// через директивы модули обмениваются функциональностью, обмен функциями
// для этого надо указать в импорте в фигурной скобке наименование передаваемого файла
// return - Оператор может возвращать значение, передавая его вызывающей функции
